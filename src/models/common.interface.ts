/**
 * Interface for result
 */
interface ReturnObject {
    success: boolean;   /* boolean for if the task was successful or not */
    message: string;    /* string for the message of the task            */
}

export {
    ReturnObject
};
